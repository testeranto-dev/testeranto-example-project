// Main entry point for the Rust project
// This could use the calculator library if needed

fn main() {
    println!("Hello, Rust project!");
    // Example of using the calculator if we wanted to:
    // use rust_calculator::Calculator;
    // let calc = Calculator::new();
    // println!("Created a calculator: {:?}", calc);
}
